package hotel;

class Payment {
	
	 public boolean processPayment(String guestName, double amount) {
	     System.out.println("Payment processed for guest: " + guestName + " Amount: " + amount);
	     return true;
	 }
	}
